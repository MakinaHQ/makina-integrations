name: Release Rootfiles

# Fires when a GitHub Release is published. Transpiles every caliber,
# detects which ones' output actually changed vs their newest existing
# rootfile, runs `check` plus the on-chain validators for the changed set,
# and opens a PR (as the release bot) with the regenerated rootfiles for a
# human to review and approve. If nothing changed, no PR is opened.
#
# The PR requires a human approval before it can merge — main's ruleset sets
# required_approving_review_count: 1. Auto-merge is enabled on top of that, so
# it means "merge once a reviewer has approved and the checks are green", not
# "merge unattended". That review matters: these rootfiles are what spellcaster
# applies, so each becomes the authoritative instruction root for its caliber,
# and rootfiles-guard only proves they are reproducible transpiler output — a
# person still has to decide the change is *wanted*.
#
# See docs/superpowers/specs/2026-07-24-release-generated-rootfiles-design.md
#
# Required repo config (see the operational setup checklist):
#   - secrets.RELEASE_BOT_TOKEN  (GitHub App installation token or fine-grained PAT;
#     contents:write + pull-requests:write on this repo)
#   - vars.RELEASE_BOT_LOGIN     (the login RELEASE_BOT_TOKEN authenticates as)
#   - secrets.ETHERSCAN_API_KEY, secrets.ALCHEMY_API_KEY (already present)
#   - "Allow auto-merge" enabled on the repo (the PR still waits for the
#     ruleset's required approval before auto-merge can land it)

on:
  release:
    types: [published]

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: release-rootfiles-${{ github.event.release.tag_name }}
  cancel-in-progress: false

jobs:
  release-rootfiles:
    name: Transpile and commit release rootfiles
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout config repo
        uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
          token: ${{ secrets.RELEASE_BOT_TOKEN }}

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Compute release identifiers
        id: release
        env:
          TAG_NAME: ${{ github.event.release.tag_name }}
          PUBLISHED_AT: ${{ github.event.release.published_at }}
        run: |
          set -euo pipefail
          SLUG=$(echo "$TAG_NAME" | tr '[:upper:]' '[:lower:]' | sed -E 's/[^a-z0-9]+/-/g' | sed -E 's/^-+|-+$//g')
          if [ -z "$SLUG" ]; then
            echo "::error::Release tag '$TAG_NAME' contains no alphanumeric characters, so it cannot be turned into a rootfile name or branch name. Re-tag the release using a name containing letters or digits." >&2
            exit 1
          fi
          TIMESTAMP=$(date -u -d "$PUBLISHED_AT" +%Y%m%d)
          echo "slug=$SLUG" >> "$GITHUB_OUTPUT"
          echo "timestamp=$TIMESTAMP" >> "$GITHUB_OUTPUT"
          echo "branch=release/rootfiles/$SLUG" >> "$GITHUB_OUTPUT"

      - name: List all calibers
        id: all
        run: |
          set -euo pipefail
          CALIBERS=$(uv run python scripts/compute_affected_calibers.py --all)
          {
            echo "calibers<<RELEASE_ALL_CALIBERS_EOF"
            echo "$CALIBERS"
            echo "RELEASE_ALL_CALIBERS_EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Transpile all calibers and detect changed output
        id: transpile
        uses: ./.github/actions/transpile-validate
        with:
          calibers: ${{ steps.all.outputs.calibers }}
          etherscan-api-key: ${{ secrets.ETHERSCAN_API_KEY }}
          alchemy-api-key: ${{ secrets.ALCHEMY_API_KEY }}

      - name: Rename changed rootfiles and clean up unchanged temp files
        id: rename
        env:
          CHANGED: ${{ steps.transpile.outputs['changed-calibers'] }}
          TIMESTAMP: ${{ steps.release.outputs.timestamp }}
          SLUG: ${{ steps.release.outputs.slug }}
        run: |
          set -euo pipefail
          renamed=()
          while IFS= read -r pair; do
            [ -z "$pair" ] && continue
            machine="${pair%%/*}"
            chain="${pair#*/}"
            dir="machines/$machine/$chain/rootfiles"
            final="$dir/${TIMESTAMP}-${SLUG}.toml"
            mv "$dir/__ci_generated__.toml" "$final"
            renamed+=("$final")
            echo "Regenerated $final"
          done <<< "$CHANGED"

          find machines -name '__ci_generated__.toml' -delete

          if [ "${#renamed[@]}" -eq 0 ]; then
            echo "has_changes=false" >> "$GITHUB_OUTPUT"
          else
            echo "has_changes=true" >> "$GITHUB_OUTPUT"
            {
              echo "files<<RELEASE_RENAMED_FILES_EOF"
              printf '%s\n' "${renamed[@]}"
              echo "RELEASE_RENAMED_FILES_EOF"
            } >> "$GITHUB_OUTPUT"
          fi

      - name: No changes — nothing to release
        if: steps.rename.outputs.has_changes == 'false'
        run: echo "No caliber's transpiled output changed since the last release. Nothing to commit."

      - name: Commit, push, and open the release PR
        if: steps.rename.outputs.has_changes == 'true'
        env:
          GH_TOKEN: ${{ secrets.RELEASE_BOT_TOKEN }}
          TAG_NAME: ${{ github.event.release.tag_name }}
          BRANCH: ${{ steps.release.outputs.branch }}
          FILES: ${{ steps.rename.outputs.files }}
          GPG_PRIVATE_KEY: ${{ secrets.RELEASE_BOT_GPG_KEY }}
          BOT_EMAIL: ${{ vars.RELEASE_BOT_EMAIL }}
        run: |
          set -euo pipefail

          # main's ruleset has a required_signatures rule, so the bot's commit must
          # be GPG-signed AND verifiable, or the PR is blocked with every check green
          # (mergeable=MERGEABLE, state=BLOCKED). Repo admins can tick the ruleset
          # bypass checkbox to force it through, but nobody should have to: an
          # unsigned release commit means this step is broken.
          #
          # "Verifiable" needs two things to line up: the signing key must be
          # registered on the bot's GitHub account, and the commit author email must
          # be an address GitHub considers verified for that account. Hence
          # RELEASE_BOT_EMAIL (the account's <id>+<login>@users.noreply.github.com
          # form) rather than an invented address — a made-up address signs fine and
          # still shows Unverified.
          #
          # gpg-agent's socket lives under GNUPGHOME, and a long path exceeds the
          # UNIX socket limit, so keep GNUPGHOME short.
          export GNUPGHOME
          GNUPGHOME=$(mktemp -d /tmp/gnupg.XXXXXX)
          chmod 700 "$GNUPGHOME"
          # trailing newline matters: a secret stored without one can trip armor parsing
          printf '%s\n' "$GPG_PRIVATE_KEY" | gpg --batch --quiet --import
          SIGNING_KEY=$(gpg --list-secret-keys --with-colons | awk -F: '/^fpr:/{print $10; exit}')
          if [ -z "$SIGNING_KEY" ]; then
            echo "::error::RELEASE_BOT_GPG_KEY did not import a usable secret key."
            exit 1
          fi
          echo "Signing as $BOT_EMAIL with key $SIGNING_KEY"

          git config user.name "release-rootfiles-bot"
          git config user.email "$BOT_EMAIL"
          git config user.signingkey "$SIGNING_KEY"
          git config commit.gpgsign true
          git config gpg.program gpg

          RENAMED=()
          while IFS= read -r file; do
            [ -n "$file" ] && RENAMED+=("$file")
          done <<< "$FILES"

          git checkout -b "$BRANCH"
          git add -- "${RENAMED[@]}"
          git commit -S -m "chore(release): rootfiles for ${TAG_NAME}"

          # Fail loudly here rather than leaving a blocked PR for someone to debug.
          if ! git verify-commit HEAD 2>/dev/null; then
            echo "::error::The release commit is not signed — main's required_signatures rule would block it."
            exit 1
          fi

          git push origin "$BRANCH"

          BULLETS=$(printf -- '- `%s`\n' "${RENAMED[@]}")
          BODY=$(printf 'Rootfiles regenerated for release `%s`.\n\n**This PR needs a human approval before it can merge** (main requires one review). Auto-merge is on, so approving it is enough — it will land once the checks are green. These rootfiles are what spellcaster applies, so each one becomes the authoritative instruction root for its caliber.\n\n## Regenerated calibers\n\n%s\n## What to check\n\n- Each caliber above is one you expected this release to touch. A caliber here that you did not knowingly change means its source drifted from its last rootfile.\n- The diff for each is the intended change and nothing more.\n- `rootfiles-guard` has already re-transpiled every file here and confirmed it is byte-identical to fresh transpiler output, so these are reproducible — review is about *whether the change is wanted*, not whether the bytes are right.\n' "$TAG_NAME" "$BULLETS")
          gh pr create --title "chore(release): rootfiles for ${TAG_NAME}" --body "$BODY" --base main --head "$BRANCH"

          gh pr merge "$BRANCH" --auto --squash
          echo "::notice::Opened the release rootfiles PR and enabled auto-merge. It still needs a reviewer's approval before it can land."
