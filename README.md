# Makina Rootfiles

## Project Structure

- every machine gets it's own toplevel directory for it's rootfiles and instructions
- `blueprints` contains instructions used across multiple machines

## Formatting

1. [install dprint](https://dprint.dev/install/)
2. run `dprint fmt` in the project root
